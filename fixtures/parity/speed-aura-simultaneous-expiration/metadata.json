{
  "fixtureVersion": 1,
  "scenarioId": "speed-aura-simultaneous-expiration",
  "description": "Speed Aura applies attack-speed and cast-speed Buffs in one cast; both schedule player-owned expiration checks at the same timestamp and are removed in a stable event order.",
  "contractVersion": 1,
  "referenceEngine": "reference-js",
  "referenceEngineVersion": "1.0.28",
  "dataVersion": "repository-v1.0.28",
  "status": "request-baseline",
  "assertions": {
    "resultEquals": {
      "numberOfPlayers": 1,
      "isDungeon": false,
      "isLabyrinth": true
    },
    "minimumRandomDraws": 50,
    "minimumTraceEvents": 50,
    "requiredTraceEventTypes": [
      "abilityCastEndEvent",
      "checkBuffExpiration"
    ]
  },
  "expectedFiles": ["request.json"],
  "notes": [
    "Enemy Impale also creates same-time expiration groups, so the integration test explicitly scopes ordering to checkBuffExpiration events whose source is player1.",
    "The detailed trace requires two Speed Aura Buffs before the first player expiration event and zero immediately after one event in the consecutive pair."
  ]
}
